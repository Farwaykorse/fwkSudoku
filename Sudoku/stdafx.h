// stdafx.h : include file for standard system include files,
// or project specific include files that are used frequently, but
// are changed infrequently
// = precomiled headers
//#pragma once

#include "targetver.h"	// Windows only

#include <stdio.h>		// Console functionality
#include <tchar.h>		// Windows only, string macros see TCHAR type
#include <cassert>		// Identify logic errors in debug builds using assert(expr)

// TODO: reference additional headers your program requires here